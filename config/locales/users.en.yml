en:
  users:
    index:
      title: Users
      id: ID
      full_name: Full name
      edit: Edit
      delete: Remove
      delete_confirm: Are you sure?
      add: + Add user
    new:
      title: Add user
    create:
      success: User was successfully created
    edit:
      title: Edit contact
    update:
      success: User was successfully updated
    show:
      title: User information
    destroy:
      success: User was successfully destroyed
    share:
      fail: Failed to share user contacts
      success: User contacts shared succefully
    share_form:
      share: Share
    form:
      phones: Phones
      emails: E-mails
      add: "+ Add"
    phone_fields:
      remove: "- Remove"
    email_fields:
      remove: "- Remove"
  activerecord:
    errors:
      models:
        user:
          attributes:
            first_name:
              blank: First name can't be blank
            last_name:
              blank: Last name can't be blank
            full_name:
              blank: Full name can't be blank
              taken: Full name should be uniq